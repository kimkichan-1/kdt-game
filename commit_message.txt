feat: 방 생성자만 게임 시작 버튼 제어 및 모든 플레이어 준비 상태 확인

- `public/index.html`: 대기실에 "게임 시작" 버튼 추가 및 초기 숨김 처리
- `public/main.js`:
    - `startGameButton` 요소 참조 추가
    - `isRoomCreator` 변수를 추가하여 현재 클라이언트가 방 생성자인지 추적
    - `socket.on('roomCreated')`에서 방 생성자에게 "게임 시작" 버튼 표시
    - `socket.on('updatePlayers')`에서 모든 플레이어가 준비 상태일 때 "게임 시작" 버튼 활성화/비활성화 제어
    - "게임 시작" 버튼에 이벤트 리스너를 추가하여 서버에 `startGameRequest` 이벤트 전송
- `server.js`:
    - `ready` 이벤트 핸들러를 수정하여 모든 플레이어가 준비 상태일 때 자동으로 게임을 시작하지 않고, 방 생성자에게 `allPlayersReady` 이벤트를 보내도록 변경
    - `startGameRequest` 이벤트 핸들러를 추가하여, 요청을 보낸 클라이언트가 방 생성자인지 확인하고 모든 플레이어가 준비 상태인지 다시 확인한 후 `startGame` 이벤트를 보내도록 구현