feat: 피격 시 캐릭터 붉은빛 효과 추가

- `public/player.js`:
    - `Player` 클래스에 `originalMaterials` 속성을 추가하여 캐릭터의 원래 재질을 저장합니다.
    - `LoadModel_` 함수에서 모델 로드 후, 캐릭터 메시의 원래 재질을 저장합니다.
    - `TakeDamage` 메서드에서 데미지를 받았을 때 캐릭터 메시의 재질 색상을 붉은색으로 변경하고, 짧은 시간 후에 원래 재질로 복원하는 로직을 추가합니다.