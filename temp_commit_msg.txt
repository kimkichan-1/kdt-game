fix: 원격 플레이어 무기 장착 동기화 버그 수정

- `public/player.js` 수정: `EquipWeapon` 메서드에서 로드된 `weaponModel`의 `userData`에 `weaponName`을 저장하도록 수정.
- `public/main.js` 수정: `RAF` 함수에서 `gameUpdate` 이벤트 발생 시 `equippedWeapon` 정보를 `this.player_.currentWeaponModel.userData.weaponName`을 통해 가져오도록 수정.